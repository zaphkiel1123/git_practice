# ES Mini NN Model — Python Dependencies
#
# Install with: pip install -r requirements_nn.txt
#
# For Intel XPU (GPU) support, install PyTorch XPU build separately:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/xpu
#   pip install intel-extension-for-pytorch

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
torch>=2.1.0
pyarrow>=14.0.0
