# ES Mini NN Model — Python Dependencies
#
# Install with: pip install -r requirements_nn.txt
#
# For Intel XPU (GPU) support, install PyTorch with +xpu build:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/xpu
#
# intel-extension-for-pytorch (IPEX) is OPTIONAL — not required if using
# a PyTorch +xpu build (e.g. torch 2.13.0+xpu). Only install if available
# for your platform for additional kernel optimizations.

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
torch>=2.1.0
pyarrow>=14.0.0
